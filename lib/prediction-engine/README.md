# Prediction Engine Skeleton

TODO: implement the real v0.1 statistical engine in a later phase.

Planned modules:

- `normalize.ts`
- `team-power.ts`
- `expected-goals.ts`
- `poisson.ts`
- `markets.ts`
- `confidence-risk.ts`
- `generate-prediction.ts`

The LLM must never decide match outcomes. It should only receive calculated prediction JSON and generate narrative.
